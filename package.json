{
  "name": "game",
  "version": "0.1.0",
  "homepage": "https://autopp.github.io/tic-tac-toe",
  "private": true,
  "dependencies": {
    "boardgame.io": "^0.22.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-zero": "^0.2.0",
    "gh-pages": "^1.1.0"
  }
}
